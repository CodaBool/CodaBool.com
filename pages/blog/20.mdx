import Layout from '../../components/Layout'
export default ({ children }) => {
  const meta = {
    "title": "Three.js Spinner",
    "excerpt": "After playing the game Rust, I loved using a spinner item and wanted to port it to the browser with Three.js",
    "created": "2021-11-20 22:20:00",
    "cover": "/assets/coverImg/spinner.jpg",
    "tags": "Three.js React Blender",
    "number": "20"
  }
  return <Layout meta={meta}>{children}</Layout>
}

# 🚧 Coming Soon